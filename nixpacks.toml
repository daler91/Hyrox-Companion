[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x", "openssl"]

# Disable the Caddy reverse proxy phase — our Express server handles HTTP directly
[phases.caddy]
nixPkgs = []
cmds = []

[start]
cmd = "echo 'NIXPACKS_START: begin' && which node && node script/start.js"
