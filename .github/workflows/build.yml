name: Build
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v4
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: pnpm
      # --ignore-scripts keeps dependency lifecycle scripts from executing in
      # CI. It also skips this repo's own postinstall, which only patches the
      # vendored deps inside the Cypress binary — nothing this job touches.
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - name: ESLint
        run: pnpm eslint .
      - name: TypeScript
        run: pnpm check
      - name: TypeScript (noUncheckedIndexedAccess ratchet)
        # Locks in noUncheckedIndexedAccess for the isomorphic shared/ layer.
        # Expand tsconfig.strict.json's include (server → client → script) as
        # each directory is cleaned up; see CONTRIBUTING.md.
        run: pnpm check:strict
      - name: TypeScript (test suite)
        # The main tsconfig excludes **/*.test.ts, so until now no job
        # typechecked the ~360 test files at all. tsconfig.test.json carries a
        # shrinking exclude list of pre-existing offenders; everything else —
        # including every newly added test — is checked here.
        run: pnpm check:test
      - name: OpenAPI snapshot is up-to-date
        run: |
          pnpm docs:openapi
          git diff --exit-code docs/openapi.json || {
            echo "::error file=docs/openapi.json::docs/openapi.json is stale. Run 'pnpm docs:openapi' locally and commit the result." >&2
            exit 1
          }

  # SonarQube Automatic Analysis is enabled for this project in SonarQube Cloud.
  # If you prefer manual CI scans, disable Automatic Analysis in SonarQube Cloud
  # and uncomment the scan step below.
  # sonarqube:
  #   name: SonarQube
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
  #       with:
  #         fetch-depth: 0
  #     - name: SonarQube Scan
  #       uses: SonarSource/sonarqube-scan-action@a31c9398be7ace6bbfaf30c0bd5d415f843d45e9 # v7.0.0
  #       with:
  #         projectBaseDir: .
  #         args: >
  #           -Dproject.settings=sonar-project.properties
  #       env:
  #         SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
