node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
cypress/videos
cypress/screenshots
cypress/downloads

# Stray CLI output / drafts occasionally captured during reviews (S13)
tsc_utf8.txt
eslint_utf8.txt
pr_desc.md
# Environment variables
.env
.env.*
!.env.example

# Replit config (project no longer uses Replit) — these embedded secrets in a
# [userenv] block, so keep them out of the repo. Set secrets as host env vars.
.replit
replit.nix
replit.md

# Testing coverage
coverage/

# Original files (e.g. from git merge or formatting tools)
*.orig

# Node compile cache
node-compile-cache/

# Generated reports
eslint_report*.json
test_report.json
vitest_report.json

# Placeholder files
dummy.png

# Stale lock files (project uses pnpm)
package-lock.json

# Backup files
*.bak

# Runtime logs
*.log

# Temp/output files
*_output.txt
tmp_*.txt

# Development artifacts
attached_assets/
pr_description.md
node_modules/

# Regenerable race-benchmark reports (the *.generated.ts artifacts ARE committed; these are not)
script/race-benchmarks/output/
node_modules/
node_modules/
node_modules/
node_modules/
node_modules/
.pnpm-store
node_modules/
.pnpm-store
